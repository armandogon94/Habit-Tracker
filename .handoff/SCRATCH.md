# Scratch — Loose Threads, Half-Formed Ideas, Debugging Breadcrumbs

> Rewritten 2026-06-04 alongside `HANDOFF.md`. Combines the still-valid 2026-05-13 notes with this session's orientation findings. Anything here is informal — pick what's useful.

---

## Things I noticed but didn't act on

### From 2026-05-13 (still valid)
- **`.claude/memory.md` says "Last updated: 2026-04-02"** but Dockerfiles were added Apr 7 and `PORTS.md` Apr 13 — memory is stale on the "no Dockerfiles" line. Worth updating during a future session.
- **`backend/.venv/`** is committed-adjacent (untracked but present). Backend `.gitignore` should ensure it's not committed; verify before any commit that touches `backend/`.
- **No `frontend/.gitignore`** — relies on root `.gitignore`. Fine, but worth confirming `node_modules/`, `.next/`, `.env.local` are excluded (they are).
- **`.claude/settings.json` is untracked.** Is this a `.local`-style file that shouldn't be committed, or did the original commit miss it? `.gitignore` doesn't mention it. User's call.
- **`PORTS.md` and `PORT-MAP.md` both exist** — slight redundancy. `PORT-MAP.md` is the global cross-project authority; `PORTS.md` is project-specific. Confirmed by reading them.

### New (2026-06-04)
- **`ios/HabitTracker/Info.plist`** showed up in the file listing — generated by `xcodegen` alongside the xcodeproj. Not in the original "Files Touched" list. Add to mental model; do not hand-edit.
- **`project.yml`** has `XCODE_VERSION: "16.0"` — a leftover assumption from the 2026-05-13 session that pre-dated Xcode 26.x being installed. xcodegen treats `XCODE_VERSION` as informational rather than enforced, so this likely doesn't block a build, but it's misleading. Consider removing or bumping to `"26.0"` during the Task 0.1 commit (single-line edit).

## Open design questions for later slices (not blocking)

- **Theme switching animation:** instant vs. crossfade? Sibling 04 uses instant via `@Observable` — do the same unless it looks janky.
- **HabitDetail heatmap on iOS:** Canvas + Path (lower-level, full control) vs. Swift Charts grid (faster, less flexible). Recommend Canvas — matches the custom SVG approach the web already uses.
- **Live Activity dismiss behavior:** auto-dismiss on habit complete vs. require user to swipe? Auto-dismiss feels right.
- **Widget sizes:** small (single habit), medium (3-4 habits), large (today's full list)? V1 should ship at least small + medium.
- **Notification sound:** default vs. custom? Default for v1.
- **Empty state** for Today when user has zero habits. Recommend a single CTA card "Tap + to create your first habit."

## Debugging breadcrumbs from prior sessions

- 2026-05-13: `xcodebuild -version` failed with: `xcode-select: error: tool 'xcodebuild' requires Xcode, but active developer directory '/Library/Developer/CommandLineTools' is a command line tools instance`. Fix was the `sudo xcode-select -s` switch in HANDOFF §7. Verified 2026-06-04 — already resolved.
- 2026-05-13: `xcodegen generate` ran clean from `ios/` directory — output:
  ```
  ⚙️  Generating plists...
  ⚙️  Generating project...
  ⚙️  Writing project...
  Created project at .../ios/HabitTracker.xcodeproj
  ```
- 2026-05-13: The Bash tool's cwd reset back to `02-Habit-Tracker` after some commands — when reading sibling 04, use absolute paths.

## New breadcrumbs (2026-06-04)

- `xcode-select -p` → `/Applications/Xcode.app/Contents/Developer` (no longer CLT).
- `xcodebuild -version` → `Xcode 26.4 / Build version 17E192`.
- `xcrun simctl list runtimes` filtered to iOS 26 → only `iOS 26.4 (26.4 - 23E244) - com.apple.CoreSimulator.SimRuntime.iOS-26-4`. **No iOS 26.0 platform installed.**
- `xcrun simctl list devices` available iPhones on iOS 26.4:
  ```
  iPhone 17 Pro       (4ECCEBC2-AA39-404A-9854-9FBB78E1A790)
  iPhone 17 Pro Max   (6E658273-3B0F-4C8E-8C4D-FF5FC9AA1CA6)
  iPhone 17e          (3D950FD4-E6F0-41AF-AF80-7574A06529C1)
  iPhone Air          (23C48E74-0BB4-42EE-9E9E-97DC448285E8)
  iPhone 17           (678EB0C0-45C4-4265-91C5-962A74D80271)
  ```
  No iPhone 16 Pro. Original `'iPhone 16 Pro,OS=26.0'` destination string will fail.

## Half-formed idea: cross-project shared theme package

Both `04-Finance-Tracker` and (now) `02-Habit-Tracker` will have nearly identical theme code (Liquid Glass + Health Cards + `AppTheme` protocol). After both ship, consider extracting `ThemeKit` as a local Swift Package shared between both iOS projects. NOT worth doing pre-emptively — duplicate first, abstract later (per `code-simplification` skill).

## Half-formed idea: backend mobile auth could enable a future Watch app

The Keychain-stored refresh token pattern (Slice 02) generalizes to a Watch app since Watch can share a Keychain group. Worth noting for v2 scope discussion.

## Half-formed idea: pin XcodeGen + Xcode version in project.yml comments

Right now `project.yml` doesn't explain *why* `XCODE_VERSION` is set or what to do when the installed Xcode is newer. A short header comment ("informational — bump on major Xcode jumps") would save the next person a confused minute.

## Tone preferences inferred from user

- Likes terse but structured replies.
- Likes recommendations stated upfront ("My rec: X") with rationale.
- Likes copy-paste-ready commands.
- Likes `→` arrows and tables for compact info.
- Doesn't want emojis unless asked.
- Wants decisions surfaced as questions before I act, not after — which is why I held the destination string change this session.

## Skill-invocation patterns that worked

- Loading skills via the `Skill` tool BEFORE doing the work, even when they overlap (per global CLAUDE.md "invoke ALL applicable skills").
- For `/build` — primary `incremental-implementation` + `test-driven-development` was sufficient for the scaffolding task. Full SwiftUI patterns load will be needed for Task 0.2 onward.

## Things to NOT redo in the next session

- **Don't re-read all 12 slice plans cold.** Read `plans/000-OVERVIEW.md` first for the index, then jump to the specific slice file you're executing.
- **Don't re-read sibling 04 in full.** Only the specific theme/feature file you're mirroring.
- **Don't re-create `SPEC.md` or `plans/`.** They're done. Update if scope changes; otherwise reference.
- **Don't re-run the `sudo xcode-select` switch.** Already done. Confirm with `xcode-select -p` before re-running.

## Risks I'm carrying that aren't in HANDOFF

- **Memory drift between Claude sessions.** If the next session uses a smaller context model or aggressive compaction, the rich SPEC + 12 plan files might be too much to load all at once. Use the `context-engineering` skill to load only the slice file in scope for any given task.
- **Sibling 04 might evolve.** If user worked on `04-Finance-Tracker` between now and the next 02 session, the theme files I'm mirroring may have changed shape. Re-read at task start.
- **iOS 26.0 vs 26.4 SDK feature drift.** If a Slice 06+ widget/Live-Activity API exists only on 26.x, the 26.0 deployment target may need to bump. Not blocking today.

## TODO for next session if it has time

- Fold the `.claude/known-issues.md` items into individual slice plans where they belong (#1 → slice 01, #2 → slice 04, etc.). Currently the cross-reference is in `SPEC.md` §11 only.
- Consider adding a `make ios` target to root `Makefile` for `cd ios && xcodegen && open HabitTracker.xcodeproj`.
- Update `.claude/memory.md` with the v2 pivot decision and reference to `SPEC.md`.
- Verify the `~/.claude/skills/` skill names referenced in `SPEC.md` §10 actually all exist on the new machine. The `liquid-glass-design`, `ios-hig-design`, `swiftui-patterns`, etc. names came from the available-skills list at original session start.
- After Task 0.1 builds green, decide whether to delete or update the stale `XCODE_VERSION: "16.0"` in `project.yml`.

## What this session did NOT do

- Did NOT modify any project files (per user's "Do NOT modify any existing project files" rule for the handoff write).
- Did NOT run `xcodebuild build` or `xcodebuild test` — held for user confirmation on the destination string.
- Did NOT commit anything.
- Did NOT touch `04-Finance-Tracker`.
